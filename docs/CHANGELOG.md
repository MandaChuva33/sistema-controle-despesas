# Histórico de Alterações

## v0.0.1

### Adicionado
- Estrutura inicial do projeto.
- Menu principal em `src/Main.java` com `println` para cada funcionalidade.
- Criação da estrutura de diretórios `src` e `docs`.
- Arquivo `docs/README.md` com a estratégia de construção e informações do autor.
- Arquivo `docs/CHANGELOG.md`.

## v0.0.2

### Adicionado
- Classes de modelo e POO: `Despesa.java` (Interface `Pagavel`, Classe Abstrata `Despesa`, Classe Concreta `DespesaAlimentacao`, Classes `TipoDespesa` e `Usuario`).
- Classes de gerenciamento: `GerenciadorDespesas.java` (com os DAOs internos).
- Documentação detalhada das classes em `docs/CLASSES.md`.
- Aplicação dos conceitos de POO (Herança, Polimorfismo, Sobrecarga, Estáticos, Criptografia).

## v0.0.3

### Adicionado
- Arquivo `README.md` na raiz do projeto com a descrição geral e estrutura.
- Arquivo `.gitignore` para ignorar arquivos de compilação e dados.
- Finalização da documentação e preparação para a entrega final.

### Adicionado
- Classes de modelo e POO: `Despesa.java` (Interface `Pagavel`, Classe Abstrata `Despesa`, Classe Concreta `DespesaAlimentacao`, Classes `TipoDespesa` e `Usuario`).
- Classes de gerenciamento: `GerenciadorDespesas.java` (com os DAOs internos).
- Documentação detalhada das classes em `docs/CLASSES.md`.
- Aplicação dos conceitos de POO (Herança, Polimorfismo, Sobrecarga, Estáticos, Criptografia).

### Adicionado
- Estrutura inicial do projeto.
- Menu principal em `src/Main.java` com `println` para cada funcionalidade.
- Criação da estrutura de diretórios `src` e `docs`.
- Arquivo `docs/README.md` com a estratégia de construção e informações do autor.
- Arquivo `docs/CHANGELOG.md`.
